# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.


I’d like to make a small app that is based on a new line of ziggu puzzles that is described in the attached academic paper as a pdf file.  More specifically, I’d like to create an app that simulates the ziggu mazes that are described in Section 4 of the attached paper. 

A visual guide is also attached in the form of a presentation. I'd like the UI to appear similar to slide 33, title Ziggu Mazes. The example maze is given but the app should be able to take any starter maze shape then simulate the puzzle for n connected mazes. The rules governing their interactions (ex. Column maze 2 equals row maze 1) stays the same. 

I want to host this to show my classmates. if there is way you can host it for free then do so. otherwise, generate the code as a google app script, with html and i can host it. I'd prefer if you handle all of that. for testing purposes we can do local.
